file(GLOB_RECURSE COMPONENT_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
file(GLOB_RECURSE COMPONENT_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.hpp")

target_sources(ohao_core PRIVATE ${COMPONENT_SOURCES} ${COMPONENT_HEADERS})
 
target_include_directories(ohao_core PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}) 