file(GLOB_RECURSE PHYSICS_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
file(GLOB_RECURSE PHYSICS_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.hpp")

target_sources(ohao_core PRIVATE ${PHYSICS_SOURCES} ${PHYSICS_HEADERS})
 
target_include_directories(ohao_core PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}) 