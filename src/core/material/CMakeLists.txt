file(GLOB_RECURSE MATERIAL_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")
file(GLOB_RECURSE MATERIAL_HEADERS "${CMAKE_CURRENT_SOURCE_DIR}/*.hpp")

target_sources(ohao_core PRIVATE ${MATERIAL_SOURCES} ${MATERIAL_HEADERS})

target_include_directories(ohao_core PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}) 