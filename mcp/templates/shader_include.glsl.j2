{# Shared GLSL include — generated from OHAO engine template #}
#ifndef {{ guard }}_GLSL
#define {{ guard }}_GLSL

// {{ description }}

{% for fn in functions %}
{{ fn.return_type }} {{ fn.name }}({{ fn.params }}) {
    {{ fn.body }}
}

{% endfor %}
{% if not functions %}
// TODO: Add functions for {{ name }}
{% endif %}

#endif // {{ guard }}_GLSL
